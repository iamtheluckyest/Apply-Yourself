{
  "name": "@customstyles/bootstrap",
  "version": "1.0.0",
  "description": "custom bootstrap for Apply Yourself app",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p build; sass --scss index.scss build/index.css",    
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "4.0.0-beta"
  }
}
